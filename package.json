{
  "name": "resume-api",
  "version": "1.0.0",
  "description": "REST API using resume sections as api endpoints",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Emmanuel Reyes",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
